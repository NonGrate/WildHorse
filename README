It is real shit!

1) Install git

2) Cd to directory

3)touch README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/NonGrate/Wild.git
git push -u origin master

Push an existing repository from the command line

git remote add origin https://github.com/NonGrate/Wild.git
git push -u origin master

----------------------------------------------------------

To upload to github from local storage:

if (new_files) git add . || git add file_name
git commit -a -m "your message"
git push


----------------------------------------------------------

To download from github to local storage:

git pull
